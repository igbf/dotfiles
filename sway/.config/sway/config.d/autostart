# Redshift
exec gammastep -l geoclue2 -m wayland -v >/tmp/gammastep.log 2>&1

# Notification daemon
exec mako >/tmp/mako.log 2>&1

# Start polkit daemon
exec /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1

# Start Udisk daemon
exec udiskie >/tmp/udiskie.log 2>&1
